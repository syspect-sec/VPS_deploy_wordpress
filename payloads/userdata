# VPS Deploy will require a root user password and a non-root user password.
# SSH root access will be turned off, so you will need to `su` into root user
# to perform administrative tasks on the server.
# Samples
# root change_this_password
# non_root_user change_this_password
root change_this_password
non_root_user change_this_password
